# 設計書�E�製造部品特化型 EC サイト「MAKERZ Direct、E

| 頁E�� | 冁E�� |
|------|------|
| **プロジェクト名** | RecruitDirect |
| **技術スタチE��** | React (TS) / Express (Node) / SQLite3 |
| **作�E日** | 2026-04-07 |

---

## 1. シスチE��構�E
- **Frontend**: React 18 / TypeScript 4.9.5 (CLI 環墁E��換)
- **Backend**: Node.js / Express 4.18
- **Database**: SQLite3 (In-Memory / File)
- **UI/Icon**: Lucide-React / Vanilla CSS
- **Document Engine**: PDF-lib

---

## 2. チE��レクトリ構�E
```text
EC-project/
├── server/             # APIサーバ�E & SQLite3 ロジチE��
├── src/                # Frontend ソース
━E  ├── components/     # Layout, Header, Footer
━E  ├── context/        # CartContext (状態管琁E
━E  └── pages/          # Home, List, Detail, Cart
├── public/             # 静的ファイル
└── docs/               # 企画・要件・設計ドキュメンチE
```

---

## 3. 主要コンポ�Eネント設訁E

### 3.1 CartContext (context/CartContext.tsx)
- **役割**: カート�E啁E��の状態を全画面で共有、E
- **チE�Eタ構造**:
  - `CartItem`: `cartId`, `id`, `name`, `specs`, `length`, `price`, `quantity`

### 3.2 ProductDetail (pages/ProductDetail.tsx)
- **役割**: 仕様選定シミュレーター、E
- **動的ロジチE��**: 
  - `selectedSpecs`: ユーザーが選択した「征E��「材質」などを保持、E
  - `totalPrice`: `basePrice + Σ(specPrice) + dimensionModifier` めE`useEffect` で計算、E
  - **SVG Preview**: 選択中の `diameter` と `length` をパラメータとして `<rect>` の `width`, `height`, `fill` を動皁E��変更、E

### 3.3 CartPage (pages/Cart.tsx)
- **役割**: カート一覧と見積書生�E、E
- **見積発行ロジチE��**: `PDFDocument.create()` により A4 横書き�E見積書を生成。テキスト描画時に `StandardFonts.Helvetica` を使用、E

---

## 4. UI/UX 設訁E
- **カラーパレチE��**: 
  - Primary: `#003366` (信頼、精寁E
  - Accent: `#FF9900` (注目、行動)
- **タイポグラフィ**: `Noto Sans JP` (和文), `Roboto Mono` (英数・仕様値)
- **ブレークポインチE*: 768px�E�モバイル表示への刁E��替え！E

---

## 5. チE�Eタベ�Eス設訁E
- **`products`**: ID、カチE��リ、名称、基準価格、E
- **`product_specs`**: 仕様名�E�征E��材質等）、表示値、価格加算額、商品ID�E�外部キー�E�、E
